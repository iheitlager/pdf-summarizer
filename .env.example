# Copyright 2025 Ilja Heitlager
# SPDX-License-Identifier: Apache-2.0

# Flask Configuration
# Use predefined key {openssl16} to generate a 32-char hex string via env.sh
#   {openssl16}       - generate 32-char hex string (openssl rand -hex 16)
#   {base64}          - generate base64-encoded 32 random bytes
#   {uuid}            - generate UUID v4
SECRET_KEY={uuid}

# Anthropic API Configuration
# Supported formats for env.sh:
#   {VaultItem:field} - retrieve from 1Password
ANTHROPIC_API_KEY={Anthropic:credential}
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Flask Environment (development or production)
FLASK_ENV=development

# Database Configuration (optional, defaults to SQLite)
# DATABASE_URL=sqlite:///pdf_summaries.db

# Logging Configuration
LOG_LEVEL=INFO

# Cleanup Job Configuration
RETENTION_DAYS=30
CLEANUP_HOUR=3

# Rate Limiting (optional, for production with Redis)
# REDIS_URL=redis://localhost:6379
